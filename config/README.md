# Configuration

This directory is reserved for application configuration examples and schema
documentation. The core framework exposes configuration interfaces only and does
not parse YAML or any other configuration format.
