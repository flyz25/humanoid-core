# Third-Party Dependencies

The framework skeleton does not vendor robot SDKs, ROS, OpenCV, AI runtimes, or
middleware implementations. Adapter modules may introduce external dependencies
behind interfaces in future repository layers.
